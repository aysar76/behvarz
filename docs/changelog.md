# تغییرات (Changelog)

## فاز ۱۱ — آکادمی مسئله‌محور

- **مسیرهای یادگیری کوتاه** (`/academy` + `/academy/[slug]`): دوره با سطح (مقدماتی/متوسط/پیشرفته)، وضعیت (پیش‌نویس/منتشرشده/بایگانی‌شده)، برچسب‌ها، نماد، ارتباط با مسئله/تجربه مرتبط و `publishedAt`.
- **درس‌های کم‌حجم** (متنی/صوتی/ویدیویی): درس با خلاصه، محتوا، آدرس رسانه، مدت و ترتیب؛ الزامی/اختیاری؛ ناوبری درس قبل/بعد.
- **آزمونک** (سنجش): پرسش چندگزینه‌ای؛ گزینه صحیح **فقط سمت سرور** ذخیره و تصحیح می‌شود و هرگز به کلاینت نمی‌رود؛ قبولی = پاسخ درست به همه پرسش‌ها؛ `QuizAttempt` برای هر تلاش.
- **ذخیره پیشرفت**: `LessonProgress` (شروع‌نشده/در حال یادگیری/تکمیل‌شده + `quizPassed`)، `CourseEnrollment` با `completedAt`؛ تکمیل خودکار دوره پس از تکمیل همه درس‌های الزامی.
- **«ثبت کاربرد میدانی»** (بستن حلقه «آموزش → سنجش → کاربرد»): `FieldApplication` با خلاصه و نتیجه (موفق/تاحدی/ناموفق) — شاخص موفقیت فاز (≥ ۴۰٪ تکمیل‌کنندگان کاربرد ثبت کنند) به‌صورت ابزار و محاسبه‌پذیر آماده شد.
- **پیشنهاد آموزش بر اساس مسائل/مهارت‌ها**: `recommendCourses` از علایق/مهارت‌های پروفایل و برچسب مسائل/تجربه‌های کاربر، بدون Popularity.
- **مدیریت محتوا** (`/admin/academy`): ساخت/ویرایش دوره، درس و آزمونک با مجوز `academy:manage`؛ نسخه (`version`) و تاریخ بازبینی (`reviewedAt`) برای هر تغییر؛ انتشار/پیش‌نویس/حذف با قواعد (دوره منتشرشده حذف نمی‌شود).
- **آمادگی دوره پولی**: فیلد `isPaid` در مدل داده — بدون درگاه پرداخت (مطابق تصمیم تجاری تأییدشده).
- مجوزهای RBAC جدید `academy:read/learn/manage` (خواندن/یادگیری برای اعضا؛ مدیریت برای admin/super_admin).
- Migration فاز ۱۱ (`Course`, `Lesson`, `CourseQuizQuestion`, `CourseEnrollment`, `LessonProgress`, `QuizAttempt`, `FieldApplication`, `CourseTag` + ۵ enum).
- ورودی ناوبری: «آکادمی» در منوی اصلی + «آکادمی یادگیری» در منوی کاربر + «آکادمی» در منوی مدیریت.

## فاز ۱۰ — رشد و اعتبار حرفه‌ای

- **مستندسازی مدل اعتبار** (`docs/reputation-model.md`) **پیش از پیاده‌سازی**: اصل شواهد، شاخص‌های ورودی، جدول نشان‌ها، منطق «قدم بعدی»، ممنوعیت نشان رسمی/لیدربورد/خرید امتیاز.
- **الگوی مقیاس جامعه** (`docs/community-scaling.md`): چرخه فعال‌سازی استان به استان با توسعه الگوی سفیران فاز ۹، شاخص آمادگی هر استان و مدیریت همزمان چند استان (شاخص موفقیت فاز ۱۰).
- **داشبورد رشد شخصی** (`/growth`): آمار واقعی (تجربه منتشرشده، مسئله حل‌شده، پاسخ مفید، حلقه فعال، اجرای موفق توسط دیگران، تشکر حرفه‌ای)، نشان‌های مشارکت، «قدم بعدی» پیشنهادی و «ادامه فعالیت‌های نیمه‌تمام» — فقط برای خود کاربر، بدون لیدربورد.
- **نشان‌های جدید** مبتنی بر شواهد: «پاسخ‌گوی مفید» (پاسخ «مفید بود» دریافتی) و «همیار حلقه» (عضویت فعال در حلقه) — در پروفایل عمومی `/users/[id]` و داشبورد.
- **قدم بعدی** (`lib/growth.ts`): منطق ترتیبی، ساده و قابل توضیح (ادامه پیش‌نویس → ثبت تجربه → پیگیری مسئله → به‌اشتراک‌گذاری → حلقه → تکمیل پروفایل → کشف دانش) با ضد قاتل انگیزه «بی‌اثر بودن».
- دسترسی از منوی کاربر («داشبورد رشد من») و بخشی در `/me`.
- شاخه `phase/10-growth-reputation`؛ هیچ نشان رسمی و هیچ رتبه‌بندی سراسری اضافه نشد.

## فاز ۹ — پایلوت میدانی و اعتبارسنجی (بسته عملیاتی، بدون کد)

- **دروازه MVP** ارزیابی و **سبز** شد (`docs/phase-reports/mvp-gate-readiness.md`): `pnpm lint`/`typecheck`/`test:run` (۲۱۱ تست)/`build` همگی موفق؛ ۰ Blocker/Critical؛ بررسی مسیرهای حیاتی + Pilot Checklist.
- **بسته اجرای پایلوت** (`docs/pilot-plan.md` + `docs/pilot/`): طرح اجرا و معیار انتخاب استان، بسته سفیر بهورز، کارگاه حضوری ۹۰ دقیقه + اسکریپت ورود گروهی، راهنمای مصاحبه (سؤال کلیدی «راحت‌تر شدن کار»)، ردیاب هفتگی شاخص‌ها (WAU/MAU، D30، نرخ پاسخ ۴۸ساعته، تبدیل به راهکار، سفیر فعال، تأیید صریح، اثر ملموس)، لاگ شواهد اثر (≥ ۱۰ مورد)، طراحی «مأموریت ساده» (نسخه ۰ بدون کد).
- شاخه `phase/09-pilot-validation`؛ هیچ قابلیت نرم‌افزاری جدیدی بدون شواهد میدانی ساخته نشد.

## فاز ۸ — جست‌وجو، اعلان و کشف دانش

- **جست‌وجو** (`/search` + `GET /api/search`): جست‌وجوی مسائل/تجربه‌ها/حلقه‌ها/اعضا (LIKE/SQLite، بدون سرویس خارجی) با فیلتر نوع/برچسب/استان/وضعیت؛ فقط محتوای منتشرشده/قابل‌نمایش و اعضای غیرخصوصی؛ پیشنهاد برچسب‌های دنبال‌شده و Empty State مفید.
- **اعلان‌ها** (`/notifications` + `GET/POST /api/notifications`): پاسخ جدید، اشاره کنترل‌شده (`@نام`)، انتخاب راهکار، پذیرش در حلقه، دعوت/جلسه حلقه، پیشنهاد/پیام/تکمیل همیاری و نتیجه اعتراض؛ خوانده/نخوانده با شمارنده؛ خواندن تک/همه با تأیید مالکیت. `notifyUser` با کنترل تنظیمات گیرنده و جلوگیری از اعلان به خود.
- **تنظیمات اعلان** (`/notifications/settings` + `GET/PATCH /api/me/notification-preferences`): فعال/غیرفعال‌کردن هر نوع اعلان (`NotificationPreference`)؛ نمونه زنده: غیرفعال‌کردن `cooperation_message` مانع دریافت پیام‌های همکاری می‌شود.
- **کشف دانش** (`/discover` + `GET /api/discover`): مسائل مرتبط با علایق/مهارت، مسائل بی‌پاسخ، تجربه‌های برگزیده/تأییدشده، حلقه‌های همان استان و «ادامه فعالیت‌های نیمه‌تمام» (پیش‌نویس‌ها + درخواست همیاری باز) — بدون Popularity.
- **UI/ناوبری:** زنگوله اعلان با شمارنده + آیکن جست‌وجو در هدر؛ «کشف دانش» در ناوبری؛ «تنظیمات اعلان» در منوی کاربر؛ ناوبری موبایل به ۴ ستون.
- Migration فاز ۸ (`Notification`، `NotificationPreference` + enum های `NotificationType`/`NotificationTargetType`).
- `lib/notifications.ts`، `lib/mention.ts`، `lib/search.ts`، `lib/discovery.ts`، `serializers/notification.ts` و `serializers/user-search.ts` + تست‌ها.

## فاز ۷ — حکمرانی محتوا و ایمنی

- **مدیریت وضعیت حساب کاربران** (`/admin/users`): اخطار/محدودسازی/تعلیق/رفع محدودیت با دلیل؛ فیلتر بر اساس وضعیت و جست‌وجو. اقدام روی خود و `super_admin` ممنوع.
- **اجرا (Enforcement)**: کاربر معلق/محدود نمی‌تواند محتوا منتشر کند (`assertAccountCanCreate` در همه مسیرهای ثبت)؛ کاربر معلق نمی‌تواند تعامل کند (`assertAccountCanInteract` در دنبال/ذخیره/تشکر/گزارش). کاربر معلق می‌تواند وارد شود (برای دیدن وضعیت و اعتراض).
- **فرآیند اعتراض** (`/appeals`): ثبت اعتراض روی محتوای خود یا وضعیت حساب (فقط نویسنده)؛ یک اعتراض در انتظار برای هر هدف؛ داشبورد ناظر `/admin/appeals` با پذیرش/رد (بازیابی محتوا یا رفع محدودیت).
- **تاریخچه تصمیم ناظر** (`ModerationDecision` + `/admin/decisions`): هر اقدام نظارتی (hide/remove/restore/warn/restrict/suspend/lift) ثبت و قابل بازبینی است؛ Soft Delete با Restore حفظ شد.
- **مدیریت واژه‌های حساس** (`/admin/sensitive-terms`): افزودن/فعال/غیرفعال/حذف؛ `scanContentForModeration` الگوهای ثابت + واژه‌های فعال را ترکیب می‌کند (کد `managed_term`).
- **مدیریت برچسب‌ها** (`/admin/tags`): ایجاد برچسب + فعال/غیرفعال‌کردن (`Tag.isActive`).
- **گزارش همکاری**: صف رسیدگی به `PeerCooperationReport` در داشبورد ناظر + `POST /api/admin/peer-reports/[id]`.
- **کنترل Spam**: جلوگیری از گزارش تکراری در انتظار بررسی (CONFLICT).
- **بنر وضعیت حساب** بالای صفحه برای کاربران دارای وضعیت غیرفعال + لینک «اعتراض به تصمیم» در منوی کاربر.
- مجوزهای RBAC جدید `moderation:users/appeals/terms/decisions` و `tags:manage`؛ `moderation:decisions` برای content_moderator هم فعال.
- Migration فاز ۷ (`ModerationDecision`، `Appeal`، `SensitiveTerm`، `AccountStatus` + `User.accountStatus*` + `Tag.isActive`).

## فاز ۶ — حلقه‌های همیار

- **درخواست همیار** (`/peer`): ثبت درخواست (عنوان، شرح، نوع مانع، برچسب‌ها، استان) + جفت‌سازی همیاران مرتبط (`/api/peer/help-requests/[id]/suggestions`) بر اساس تمایل به همیاری، تجربه/برچسب، مهارت/علاقه، استان و کیفیت همکاری‌های قبلی — بدون Popularity.
- **پیشنهاد همیاری**: همیار پیشنهاد می‌دهد یا درخواست‌دهنده دعوت می‌کند (`PeerOffer` با کلید یکتا)؛ پذیرش/رد/پس‌گرفتن.
- **همکاری دونفره**: تعیین هدف، گفت‌وگوی محدود و موضوع‌محور Thread-Based (`PeerMessage`؛ بدون Chat بلادرنگ)، ثبت خلاصه نتیجه و خاتمه، ارزیابی سودمندی دوطرفه (۱-۵)، گزارش سوءاستفاده با دلایل تعریف‌شده.
- **حلقه‌های همیار** (`/circles`): گروه‌های کوچک ۵ تا ۱۲ نفره؛ ایجاد (خالق = راهبر)، درخواست عضویت/دعوت/تأیید، ظرفیت، صفحه حلقه، جلسات و خروجی دوره‌ای، خروج، انتقال راهبری، آرشیو.
- **فیلد `willingToHelp`** در پروفایل/Onboarding (فقط این کاربران به‌عنوان همیار پیشنهاد می‌شوند).
- مجوزهای RBAC جدید `circles:create/join/manage/meeting` و `peer:request/offer/cooperate`؛ بررسی راهبری سمت سرور؛ Audit Log؛ Rate Limit؛ پاک‌سازی محتوای حساس روی متن حلقه/درخواست.
- Migration فاز ۶ (`Circle*`، `Peer*` + ۱۲ enum + `User.willingToHelp`).
- صفحه‌های ناوبری «حلقه‌های همیار» و «همیاری» فعال شدند.

## فاز ۵ — تعاملات حرفه‌ای و سرمایه روایت (پایه)

- **دنبال‌کردن** موضوع/مسئله/تجربه/عضو (`Follow` پلی‌مورفیک) با اعتبارسنجی هدف سمت سرور؛ دنبال‌کردن عضو با پروفایل خصوصی و دنبال‌کردن خود ممنوع.
- **ذخیره محتوا** (خواندنی‌های من) برای مسائل/تجربه‌ها (`SavedItem`) + صفحه `/saved`.
- **«تشکر حرفه‌ای»** به‌جای لایک عمومی روی پاسخ و تجربه (`ProfessionalThanks`) + شمارنده تراکنشی `thanksCount`؛ تشکر به محتوای خود ممنوع.
- **پروفایل سرمایه حرفه‌ای** (`/users/[id]`): تجربه‌های منتشرشده، مسائل حل‌شده، ارجاع‌های معتبر، اجرای موفق توسط دیگران، تشکر دریافتی + **نشان‌های مبتنی بر شواهد** (بدون لایک/لیدربورد).
- **خوراک حرفه‌ای** (`/feed`): بر اساس موضوع‌ها/اعضای دنبال‌شده (مرتبط موضوعی، نه محبوبیت).
- حالت تعامل (ذخیره/دنبال/تشکر) در Serializer ها از طریق `getInteractionState`؛ دکمه‌های تعاملی در صفحات مسئله/تجربه و پاسخ.
- مجوزهای RBAC جدید `interactions:follow/save/thanks` و `profile:read:other`؛ Audit Log برای همه تعاملات.
- Migration فاز ۵ (`Follow`، `SavedItem`، `ProfessionalThanks` + `thanksCount`).
- صفحه ناوبری جدید «خوراک حرفه‌ای».

## فاز ۴ — بانک تجربه‌های میدانی

- مدل تجربه ساختاریافته: عنوان، مسئله/موقعیت، شرایط، اقدام، منابع، چالش‌ها، نتیجه، درس‌آموخته‌ها، پیشنهاد برای دیگران، برچسب‌ها + اسلاگ قابل اشتراک (`/experiences/[slug]`).
- ثبت پیش‌نویس/پیش‌نمایش/انتشار/ویرایش؛ وضعیت‌ها (user_generated → under_review → reviewed → featured → archived) با انتقال معتبر و تفکیک «تجربه شخصی» از «محتوای بررسی‌شده/برگزیده».
- **تبدیل پاسخ موفق اتاق به تجربه** (`POST /api/problems/[id]/convert-to-experience`) — چرخه بسته دانش (مسئله ← راهکار ← تجربه).
- **ارجاع تجربه در پاسخ‌ها** (`experienceSlugs`) — تعداد ارجاع بخشی از سرمایه روایت.
- **«این تجربه را اجرا کردم» + ثبت نتیجه اجرای مجدد** (`ExperienceReuse` با upsert) — سرمایه روایت مبتنی بر اثر، نه لایک.
- هشدار/کنترل محتوای حساس + صف ناظر برای تجربه‌ها؛ گزارش تجربه (`ContentReport.experienceId`) + نظارت مخفی/حذف/بازیابی.
- صفحه‌های `/experiences`، `/experiences/new`، `/experiences/[slug]` و فعال‌سازی آیتم «بانک تجربه» در ناوبری؛ بخش تجربه‌ها در صف مدیریت محتوا.
- Migration فاز ۴ (`Experience`، `ExperienceTag`، `ExperienceReference`، `ExperienceReuse` + توسعه `ContentReport`).

## فاز ۳ — اتاق مسئله (هسته محصول)

- مدل مسئله ساختاریافته: عنوان، شرح، زمینه/شرایط، نوع مانع، اقدامات انجام‌شده، نتیجه مورد انتظار، برچسب‌ها، فوریت، انتشار ناشناس، پیش‌نویس.
- وضعیت‌ها (open/discussing/solved/archived) با **تاریخچه وضعیت قابل پیگیری** (`ProblemStatusChange`).
- پاسخ ساختاریافته + «درخواست توضیح» + «مفید بود» (بدون لایک عمومی) + انتخاب راهکار با جمع‌بندی + ثبت نتیجه اجرا.
- هشدار/کنترل محتوای حساس پیش از انتشار (`content-safety.ts`)؛ محتوای مشکوک به صف بررسی ناظر می‌رود.
- گزارش محتوای نامناسب (`ContentReport`) + اقدامات نظارت (مخفی/حذف/بازیابی) توسط ناظر با Audit Log.
- تاریخ شمسی در UI (`dates.ts`) با ذخیره استاندارد UTC در دیتابیس؛ Rate Limit برای مسئله/پاسخ/گزارش.
- Migration های فاز ۳ (Problem، ProblemAnswer، Tag، ContentReport و...).
- صفحه‌های `/problems`، `/problems/new`، `/problems/[id]` و مدیریت محتوا `/admin/moderation`.

## فاز ۲ — هویت، احراز هویت و پروفایل حرفه‌ای

- ورود با شماره موبایل + OTP (Provider قابل جایگزینی؛ `devCode` فقط در توسعه)، Rate Limit، نشست امن (hash توکن + کوکی HttpOnly) و خروج از همه دستگاه‌ها (`logout-all`).
- Onboarding چهارمرحله‌ای و پروفایل حرفه‌ای: استان/شهرستان، سابقه بازه‌ای، مهارت‌ها/علایق، معرفی، Visibility.
- نقش‌ها (۸ نقش) + RBAC در Server و UI؛ درخواست و تأیید عضویت حرفه‌ای توسط مدیر.
- Audit Log برای ورود، ویرایش پروفایل و بررسی عضویت.
- Migration فاز ۲ (OtpCode، Session، MembershipRequest، Skill/Interest، AuditLog و توسعه User).
- رفع گیرنده: صفحه `/me` در `AppShell` قرار گرفت تا `ToastProvider` در دسترس باشد (خطای `useToast` رفع شد).

## فاز ۱ — بنیان فنی و سیستم طراحی (شروع)

- راه‌اندازی Next.js 16 (App Router, Turbopack) با TypeScript Strict.
- Design System: توکن‌ها، RTL، فونت Vazirmatn (self-host)، کامپوننت‌های پایه و App Shell.
- لایه دیتابیس: Prisma 7 + SQLite + adapter better-sqlite3؛ مدل پیش‌نویس User.
- ابزارهای پایه: zod (Validation)، logger، AppError، Vitest، ESLint، Prettier، CI اولیه.
- صفحات وضعیت: error / global-error / loading / not-found و پیش‌نمایش `/ui`.
