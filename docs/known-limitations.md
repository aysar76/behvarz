# محدودیت‌های شناخته‌شده (Known Limitations)

**وضعیت:** به‌روزرسانی با وضعیت واقعی کد — تاریخ: مهر ۱۴۰۵

## محدودیت‌های عمدی

| مورد                            | توضیح                                                                                                              | زمان بازبینی               |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------- |
| SQLite در توسعه                 | مقیاس/قابلیت‌های Production ندارد؛ مسیر مهاجرت به PostgreSQL در فاز ۱۳                                             | فاز ۱۳                     |
| استقرار تک‌نمونه (Single Instance) | فاز ۱۳ روی یک نمونه با SQLite استقرار می‌شود؛ Rate Limit/کش در حافظه نمونه‌اند؛ چندنمونه نیازمند PostgreSQL/Redis | پس از تصمیم Database       |
| ارسال OTP واقعی (SMS)           | Provider فقط `dev` است؛ کد در پاسخ API در حالت غیر Production دیده می‌شود                                          | فازهای بعد با زیرساخت قطعی |
| مسیرهای ناوبری                  | «اتاق مسئله»، «بانک تجربه»، «حلقه‌های همیار» و «همیاری» فعال‌اند                                                   | —                          |
| گفت‌وگوی همیاری بدون بلادرنگ    | همکاری‌ها Thread-Based و Pull هستند؛ بدون WebSocket/Chat بلادرنگ در MVP                                            | فاز ۱۳                     |
| جفت‌سازی همیار ساده             | امتیازدهی قابل توضیح در کد؛ بدون داشبورد تنظیم وزن/الگوریتم کشف کامل                                               | فاز ۸                      |
| Toast/Modal بدون آزمایش حرکتی   | MVP هیچ انیمیشن ورود/خروج ندارد (سبک و کم‌مصرف)                                                                    | هنگام نیاز                 |
| فهرست استان‌ها/مهارت‌ها ثابت    | در کد تعریف شده‌اند؛ مدیریت پویا در فازهای بعد                                                                     | فازهای بعد                 |
| گزارش‌دهی Audit بدون UI کامل    | لاگ‌ها ثبت می‌شوند؛ داشبورد ممیزی کامل در فاز ۱۰                                                                   | فاز ۱۰                     |
| تست Integration برای API        | تست‌های واحد لایه‌های منطقی موجود؛ تست Integration در فاز ۱۳                                                       | فاز ۱۳                     |
| Search/کشف دانش                 | جست‌وجوی ساده (LIKE/SQLite) و کشف مبتنی بر علایق/بی‌پاسخ/برگزیده در فاز ۸؛ Full-Text فارسی کامل و ایندکس در فاز ۱۳ | فاز ۱۳                     |
| CSRF با `APP_ORIGIN`            | در Production `APP_ORIGIN` باید دقیق تنظیم شود؛ درخواست‌های بدون `Origin`/`Referer` (curl/سروربه‌سرور) مجازند    | —                          |
| CSP شامل `'unsafe-inline'`      | سازگاری با Next/Tailwind؛ سخت‌سازی Nonce در فازهای بعد                                                            | بازبینی هنگام نیاز         |
| Restore نیازمند توقف سرویس      | بازیابی SQLite باید با برنامه متوقف انجام شود (قفل فایل)                                                           | —                          |
| Mention ساده                    | تشخیص «@نام» فقط یک‌بار برای هر کاربر و نام یک‌تکه (بدون فاصله)؛ توسعه در فازهای بعد                               | هنگام نیاز                 |
| اعلان‌ها درون‌برنامه‌ای         | فقط درون‌برنامه‌ای (Pull)؛ Provider های SMS/Push/Email با زیرساخت قطعی در فازهای بعد                               | فازهای بعد                 |
| ارجاع تجربه در پاسخ             | کاربران اسلاگ/لینک تجربه را وارد می‌کنند؛ انتخاب/جست‌وجوی بصری تجربه در فازهای بعد                                 | فازهای بعد                 |
| تصمیم ناظر بدون جزئیات هدف      | تاریخچه تصمیم‌ها فهرستی است؛ جزئیات هدف (محتوای متنی) در فاز ۱۰                                                    | فاز ۱۰                     |
| واژه‌های حساس بدون Regex        | `SensitiveTerm` فقط مطابقت «شامل‌بودن» دارد (بدون Regex از سمت مدیر برای امنیت)                                    | بازبینی هنگام نیاز         |
| Mention و اعلان‌ها              | اعلان‌ها (پاسخ/ارجاع/نتیجه/رویداد حلقه) در فاز ۸ پیاده شدند؛ فقط درون‌برنامه‌ای، بدون SMS/Push/Email               | فازهای بعد                 |
| خوراک فقط موضوعی                | خوراک بر اساس دنبال‌شده‌ها است؛ کشف/پیشنهاد کامل در فاز ۸ (روی `/discover`)                                        | —                          |
| نشان‌های سرمایه روایت ثابت      | نشان‌ها در `serializers/capital.ts` محاسبه می‌شوند؛ مدل اعتبار در `docs/reputation-model.md` مستند شد (فاز ۱۰) | بازبینی با شواهد |
| قابلیت «مأموریت ساده» ساخته نشد | فاز ۹ مأموریت را نسخه ۰ (اعلام هفتگی توسط سفیر، بدون کد) اجرا می‌کند؛ ساخت قابلیت فقط با شواهد موفقیت              | فاز ۱۰                     |
| «قدم بعدی» داشبورد رشد ساده      | منطق ترتیبی و قابل توضیح در `lib/growth.ts`؛ بدون الگوریتم مخفی/توصیه سنگین                                     | بازبینی با شواهد |
| جلسات زنده آکادمی ساخته نشد       | فاز ۱۱ «جلسات زنده» را در محدوده دارد اما برای MVP بدون WebSocket/بلادرنگ اجرا نمی‌شود؛ در backlog          | فاز ۱۳/بعد       |
| آزمونک فقط چندگزینه‌ای            | بدون سؤال تشریحی/گزینه‌ای وزنی؛ قبولی = پاسخ درست به همه پرسش‌ها (ساده و قابل توضیح)                         | بازبینی با شواهد |
| پیشنهاد دوره ساده                 | بر اساس تطابق برچسب با علایق/مهارت/مسائل است؛ بدون امتیازدهی ترکیبی/ML                                           | بازبینی با شواهد |
| محتوای رسانه بدون آپلود           | درس صوتی/ویدیویی فقط با `mediaUrl` خارجی است؛ آپلود/مدیریت رسانه داخلی در فازهای بعد                         | فازهای بعد       |
| گواهی داخلی ساخته نشد             | فاز ۱۱ «گواهی داخلی غیررسمی با هشدار» را ذکر می‌کند؛ برای جلوگیری از برداشت «مدرک رسمی» فعلاً ساخته نشد و به شواهد نیازمند است | فاز ۱۲/بعد       |
| دوره پولی فقط آماده‌سازی           | فیلد `isPaid` وجود دارد؛ بدون درگاه پرداخت (مطابق اصل «بدون درآمد بدون تصمیم تجاری»)                           | پس از تصمیم تجاری |
| شاخص کاربرد میدانی وابسته به اجرا  | عدد واقعی «≥ ۴۰٪ کاربرد میدانی» پس از وجود کاربران/دوره‌ها محاسبه می‌شود (ابزار ثبت و شمارش آماده است)        | پس از اجرا       |
| رأی‌گیری بودجه ساده                | فاز ۱۲ هر عضو یک رأی برابر دارد؛ بدون رأی وزنی/امتیازی، بازه رأی‌گیری و بدون مدیریت «واجدین شرایط» پویا (فعلاً عضویت فعال کافی است) | بازبینی با شواهد |
| ثبت استفاده مزیت بدون تأیید        | ثبت استفاده توسط خود کاربر است و به تأیید ارائه‌دهنده/مدیر نیاز ندارد؛ برای جلوگیری از بار اداری عمدی است    | بازبینی با شواهد |
| بودجه بدون کیف پول/پرداخت واقعی   | فاز ۱۲ فقط «گزارش اجرا و هزینه» را ثبت می‌کند؛ بدون کیف پول، پرداخت واقعی و سیستم مالی پیچیده (مطابق ممنوعیت)   | پس از تصمیم تجاری |
| بودجه‌ریزی بدون مرحله رأی‌گیری بازه‌دار | رأی‌گیری پایان/آغاز خودکار ندارد و توسط مدیر آغاز/بسته می‌شود؛ بدون احراز رأی با شماره/یکتا هویت             | بازبینی با شواهد |
| شاخص «≥ ۳۰٪ دریافت مزیت» وابسته به اجرا | عدد واقعی نرخ استفاده از مزایا پس از وجود ارائه‌دهندگان/کاربران واقعی محاسبه می‌شود (ابزار ثبت و شمارش آماده است) | پس از اجرا       |
| مرکز فرماندهی و نقشه موانع تجمیعی | داده‌ها در هر بارگذاری از DB محاسبه می‌شوند (بدون Cache)؛ برای توپولوژی فعلی کافی است | با مقیاس |
| نقشه موانع بدون رندر جغرافیایی | «نقشه موانع» فعلاً جدول تجمیعی «نوع مانع × استان» است؛ نقشه بصری/رندر SVG در فازهای بعد | با مقیاس/نیاز |
| کمپین‌ها فقط «مشارکت» | بازی‌های شبکه‌ای سبک‌اند: کمپین + مشارکت اختیاری؛ چالش/مأموریت با نتیجه ثبت‌شده در فازهای بعد | با شواهد |
| ابزارها متنی | ابزارهای اجرایی بدون رسانه/فایل ضمیمه و بدون جست‌وجوی داخلی | فازهای بعد |
| رضایت داده اختیاری و پیش‌فرض خاموش | `allowDataContribution` پیش‌فرض false است؛ فقط مسائل کاربران دارای رضایت در نقشه موانع لحاظ می‌شود | — |

## نکات فنی

- `next/font/local` به نسخه فایل فونت محلی وابسته است (`src/fonts/vazirmatn-variable.woff2`).
- خروجی Prisma Client (`src/generated/`) تولیدشده است و نباید ویرایش شود.
- CI (GitHub Actions) فرض می‌کند `prisma migrate` و `prisma generate` قابل اجرا هستند (نیازمند `.env` با `DATABASE_URL`).
- درخواست‌های OTP در حالت توسعه Rate Limit دارند (IP: ۱۰/۱۰دقیقه، شماره: ۳/۱۰دقیقه).
