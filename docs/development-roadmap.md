# نقشه توسعه (Development Roadmap)

**وضعیت:** نسخه اولیه برای فاز ۱ — تاریخ: مهر ۱۴۰۵

## نقشه فازها (از MASTER_PLAN)

| فاز        | عنوان                                  | افق                         |
| ---------- | -------------------------------------- | --------------------------- |
| ۰          | شناخت، ممیزی و تثبیت معماری            | پایه (تکمیل شد: greenfield) |
| ۱          | بنیان فنی و سیستم طراحی                | پایه (تکمیل شد)             |
| ۲          | هویت، احراز هویت و پروفایل حرفه‌ای     | پایه (تکمیل شد)             |
| ۳          | اتاق مسئله (هسته محصول)                | MVP (تکمیل شد)              |
| ۴          | بانک تجربه‌های میدانی                  | MVP (تکمیل شد)              |
| ۵          | تعاملات حرفه‌ای و سرمایه روایت (پایه)  | MVP (تکمیل شد)              |
| ۶          | حلقه‌های همیار                         | MVP (تکمیل شد)              |
| ۷          | حکمرانی محتوا و ایمنی                  | MVP (تکمیل شد)              |
| ۸          | جست‌وجو، اعلان و کشف دانش              | MVP (تکمیل شد)              |
| دروازه MVP | ارزیابی آمادگی + پایلوت                | دروازه (سبز شد)             |
| ۹          | پایلوت میدانی و اعتبارسنجی             | اعتبارسنجی (در جریان)       |
| ۱۰         | رشد و اعتبار حرفه‌ای                   | رشد (تکمیل شد)              |
| ۱۱ تا ۱۴   | آکادمی، مزایا، سخت‌سازی، افق بلند      | رشد/انتشار                  |

## شاخه‌های گیت

- هر فاز یک شاخه دارد: `phase/00-discovery` تا `phase/14-...`.
- فاز ۱: `phase/01-foundation`.
- فاز ۲: `phase/02-identity`.
- فاز ۳: `phase/03-problem-room`.
- فاز ۴: `phase/04-experience-bank`.
- فاز ۵: `phase/05-professional-interactions`.
- فاز ۶: `phase/06-helper-circles`.
- فاز ۷: `phase/07-content-governance`.
- فاز ۸: `phase/08-search-notifications-discovery`.
- فاز ۹: `phase/09-pilot-validation`.
- فاز ۱۰: `phase/10-growth-reputation`.

## استاندارد ساخت قابلیت جدید
1. **پیش از کدنویسی:** تعیین کنید قابلیت به کدام بخش از «حلقه بسته دانش» متصل است؛ اگر به هیچ‌کدام وصل نیست، ساخته نشود.
2. **یک قابلیت کامل:** هر قابلیت شامل: کد، تست، وضعیت Error/Empty/Loading و مستندات است.
3. **Validation سمت سرور:** هر ورودی کاربر پیش از ذخیره با schema (zod) اعتبارسنجی می‌شود.
4. **Authorization:** کنترل دسترسی سمت سرور (نه فقط UI) انجام می‌شود.
5. **RTL و Mobile-First:** هر صفحه از ابتدا RTL و Mobile-First ساخته می‌شود و فقط با کامپوننت‌های Design System.
6. **بدون داده بیمار و بدون قابلیت شبیه سیب.**
7. **چک قبل از تحویل:** `pnpm lint` و `pnpm typecheck` و `pnpm test` و `pnpm build` بدون خطا.

## جریان مرجع (حلقه بسته دانش)

```
ثبت مسئله ← دریافت پاسخ/تجربه ← انتخاب راهکار ← ثبت نتیجه ← تبدیل به تجربه ← جستجو/بازتولید ← آموزش ← اثر جدید
```
