# بدهی فنی (Technical Debt)

**وضعیت:** نسخه اولیه برای فاز ۱ — تاریخ: مهر ۱۴۰۵

مخزن در وضعیت greenfield است و بدهی فنی از کد legacy وجود ندارد. این فایل برای پیگیری بدهی‌های «عمدی» که در طول توسعه پذیرفته می‌شوند، نگهداری می‌شود.

| شناسه | بدهی                                                        | دلیل پذیرش                                           | زمان بازپرداخت پیشنهادی | وضعیت |
| ----- | ----------------------------------------------------------- | ---------------------------------------------------- | ----------------------- | ----- |
| PH3-1 | Sync برچسب‌ها و برخی به‌روزرسانی‌ها بدون Transaction فراگیر | سادگی در MVP؛ ریسک پایین (تأخیر جزئی در داده جانبی)  | فاز ۱۳                  | فعال (با PostgreSQL تراکنش‌های قوی‌تر ممکن می‌شود) |
| PH3-2 | Rate Limit در حافظه (Map) نه توزیع‌شده                      | بدون زیرساخت خارجی در MVP؛ برای نمونه اولیه کافی است | فاز ۱۳                  | فعال (برای تک‌نمونه کافی است؛ توزیع‌شده با چند نمونه در فازهای بعد) |
| PH3-3 | الگوی تشخیص محتوای حساس مبتنی بر Regex ساده                 | شروع کافی؛ مدیریت واژه‌ها در فاز ۷ (SensitiveTerm)   | فاز ۷                   | **بسته شد (فاز ۷)** |
| PH3-4 | صف بررسی ناظر (رزرو)                                        | داشبورد کامل حکمرانی در فاز ۷                        | فاز ۷                   | **بسته شد (فاز ۷)** |
| PH3-5 | «مسئله‌های مرتبط» فقط بر پایه برچسب مشترک                   | جست‌وجو/کشف کامل در فاز ۸                            | فاز ۸                   | **بسته شد (فاز ۸)** |
| PH4-1 | اسلاگ تجربه توکن تصادفی (بدون بخش خوانا)                  | محتوای فارسی قابل ترجمهٔ خودکار به اسلاگ خوانا نیست    | بازبینی در فاز ۸        | فعال  |
| PH4-2 | «تجربه‌های مرتبط» فقط بر پایه برچسب مشترک                 | جست‌وجو/کشف کامل در فاز ۸                            | فاز ۸                   | **بسته شد (فاز ۸)** |
| PH4-3 | ارجاع تجربه در پاسخ با واردکردن دستی اسلاگ (بدون جست‌وجو) | بدون زیرساخت جست‌وجو در MVP؛ انتخاب بصری در فاز ۸    | فاز ۸                   | **بسته شد (فاز ۸)** |
| PH4-4 | سرمایه روایت بدون محاسبه «استمرار اثر در زمان» | نیازمند داده تاریخی؛ در فاز ۱۰ (داشبورد رشد)          | فاز ۱۰                  | فعال  |
| PH6-1 | جفت‌سازی همیار ساده و بدون داشبورد/تنظیم وزن‌ها | سادگی و قابل توضیح‌بودن در MVP                        | فاز ۸                   | فعال  |
| PH6-2 | گفت‌وگوی همیاری Thread-Based بدون بلادرنگ (Pull) | طبق محدوده فاز؛ بدون WebSocket                       | فاز ۱۳                  | فعال  |
| PH6-3 | گزارش سوءاستفاده همیاری                                    | صف رسیدگی در فاز ۷                                   | فاز ۷                   | **بسته شد (فاز ۷)** |
| PH6-4 | شمارش عضویت از `_count` خوانده می‌شود (بدون denormalize) | سادگی در MVP؛ بهینه‌سازی در فاز ۱۳                    | فاز ۱۳                  | فعال (ایندکس موجود است؛ denormalize با مقیاس و PostgreSQL) |
| PH7-1 | تصمیم ناظر در UI به‌صورت فهرست بدون جزئیات هدف              | سادگی در MVP                                          | فاز ۱۰                  | فعال  |
| PH7-2 | واژه‌های حساس فقط «شامل‌بودن» ساده (بدون Regex از مدیر)    | امنیت در برابر Regex تزریقی                           | بازبینی هنگام نیاز      | فعال  |
| PH7-3 | `scanContentForModeration` هر بار واژه‌ها را از DB می‌خواند | بدون کش؛ ساده در MVP/SQLite                            | فاز ۱۳                  | **بسته شد (فاز ۱۳)**: کش TTL ۶۰ ثانیه‌ای (`lib/ttl-cache.ts` + `getActiveSensitiveTerms`) |
| PH7-4 | تعلیق فقط بر «انتشار/تعامل» اثر دارد؛ خواندن حفظ می‌شود    | برای امکان اعتراض کاربر معلق                          | بازبینی با خط مشی       | فعال  |
| PH8-1 | جست‌وجو با LIKE/SQLite (بدون Full-Text فارسی/ایندکس)        | سادگی و کم‌وابستگی در MVP                             | فاز ۱۳                  | فعال (با PostgreSQL و Full-Text/معماری جست‌وجو در فازهای بعد) |
| PH8-2 | اعلان‌ها فقط درون‌برنامه‌ای (Pull) و بدون WebSocket          | بدون زیرساخت قطعی؛ Provider آماده است                 | فازهای بعد              | فعال  |
| PH8-3 | Mention فقط نام یک‌تکه و یک‌بار به‌ازای کاربر               | سادگی و جلوگیری از سوءاستفاده                        | هنگام نیاز              | فعال  |
| PH8-4 | پیشنهاد کشف ثابت در کد (بدون پنل تنظیم وزن/معیار)           | قابل توضیح‌بودن و سادگی در MVP                        | فاز ۱۰                  | فعال  |
| PH8-5 | شمارش اعلان خوانده‌نشده در هر بارگذاری صفحه                 | بدون Cache؛ ساده در MVP                               | فاز ۱۳                  | فعال (با Redis/Cache توزیع‌شده در فازهای بعد) |
| PH4-4 | سرمایه روایت بدون محاسبه «استمرار اثر در زمان» | نیازمند داده تاریخی؛ در فاز ۱۰ (داشبورد رشد)          | فاز ۱۰                  | فعال  |
| PH10-1 | شاخص‌های داشبورد رشد در هر بارگذاری از DB محاسبه می‌شوند (بدون Cache/denormalize) | سادگی و هم‌خوانی همیشگی با واقعیت در MVP | فاز ۱۳ | فعال (کش/denormalize با مقیاس در فازهای بعد) |
| PH10-2 | «استمرار اثر در زمان» هنوز محاسبه نمی‌شود | نیازمند داده تاریخی پایلوت؛ پس از شواهد | فاز ۱۱+ | فعال |
| PH11-1 | آزمونک فقط چندگزینه‌ای و «قبولی = همه درست» | ساده و قابل توضیح؛ بدون وزندهی پیچیده | بازبینی با شواهد | فعال |
| PH11-2 | پیشنهاد دوره فقط تطابق برچسب (بدون امتیازدهی ترکیبی) | بدون Popularity و قابل توضیح | بازبینی با شواهد | فعال |
| PH11-3 | پرسش‌ها/پیشرفت در هر بارگذاری از DB خوانده می‌شوند (بدون Cache) | سادگی در MVP/SQLite | فاز ۱۳ | فعال (کش با مقیاس در فازهای بعد) |
| PH11-4 | ویرایش درس فقط عنوان/محتوا/نوع؛ بدون بازآرایی بصری ترتیب و مدیریت رسانه | سادگی؛ محتوا از طریق فرم‌ها مدیریت می‌شود | فازهای بعد | فعال |
| PH12-1 | رأی‌گیری بودجه بدون بازه زمانی خودکار و بدون رأی وزنی | سادگی و قابل توضیح؛ مدیر آغاز/بسته می‌کند | بازبینی با شواهد | فعال |
| PH12-2 | میانگین رضایت/شمار استفاده در هر بارگذاری از DB محاسبه می‌شوند (بدون Cache/denormalize) | هم‌خوانی همیشگی با واقعیت در MVP | فاز ۱۳ | فعال (کش با مقیاس در فازهای بعد) |
| PH12-3 | ثبت استفاده مزیت به تأیید ارائه‌دهنده نیاز ندارد (برای جلوگیری از بار اداری) | اعتماد اولیه؛ بازبینی با شواهد سوءاستفاده | بازبینی با شواهد | فعال |
| PH12-4 | «واجدین شرایط رأی» معیار ساده (عضو فعال) دارد؛ بدون هیئت‌نامه/شورا | گام اولیه بودجه مشارکتی | پس از شورای جامعه | فعال |
| PH13-1 | SQLite تک‌نمونه (بدون PostgreSQL) | استقرار پایدار فاز ۱۳ روی تک‌نمونه؛ مهاجرت به PostgreSQL «نیازمند تأیید» است | پس از تأیید تصمیم Database | فعال |
| PH13-2 | Rate Limit در حافظه (تک‌نمونه) | برای توپولوژی فاز ۱۳ کافی است؛ چندنمونه نیازمند Redis | با مقیاس/چندنمونه | فعال |
| PH13-3 | OTP واقعی (SMS) ساخته نشد | Provider فقط `dev`؛ نیازمند سرویس رسمی و قرارداد | پس از زیرساخت قطعی | فعال |
| PH13-4 | اعلان‌های بلادرنگ (WebSocket/Push) ساخته نشد | بدون زیرساخت؛ Pull درون‌برنامه‌ای کافی است | پس از زیرساخت قطعی | فعال |
| PH13-5 | جست‌وجوی Full-Text فارسی ساخته نشد | LIKE/SQLite ساده و کم‌وابستگی | با PostgreSQL/سرویس جست‌وجو | فعال |
| PH13-6 | CSP شامل `'unsafe-inline'` است | سازگاری با Next/Tailwind؛ سخت‌سازی Nonce نیازمند بازبینی | بازبینی هنگام نیاز | فعال |
| PH14-1 | مرکز فرماندهی و نقشه موانع در هر بارگذاری از DB محاسبه می‌شوند (بدون Cache) | سادگی و هم‌خوانی همیشگی با واقعیت؛ Cache با مقیاس | با مقیاس/مونیتورینگ | فعال |
| PH14-2 | «نقشه موانع» فعلاً فقط جدول شمارش؛ نقشه جغرافیایی بصری/رندر SVG ساخته نشد | سادگی در MVP و کم‌وابستگی | با مقیاس/نیاز | فعال |
| PH14-3 | بازی‌های شبکه‌ای فقط «کمپین + مشارکت» هستند؛ چالش‌ها/مأموریت‌های دارای نتیجه ثبت‌شده در فازهای بعد | نسخه سبک و اختیاری طبق فاز ۱۴ | با شواهد | فعال |
| PH14-4 | ابزارها بدون رسانه/فایل ضمیمه و بدون جست‌وجوی داخلی هستند | سادگی؛ محتوا متنی است | فازهای بعد | فعال |
| PH14-5 | عدد واقعی شاخص موفقیت فاز (اثر اکوسیستم) وابسته به استقرار/جامعه واقعی است (ابزار آماده) | فاز افق بلند پس از تثبیت شاخص‌ها | پس از اجرا | فعال |

## اصول مدیریت بدهی فنی

- هر بدهی عمدی باید اینجا ثبت و مستند شود.
- «سرعت تولید فایل معیار نیست؛ قابلیت کمتر اما کامل، امن و قابل استفاده.»
- رفع خطا مبتنی بر علت اصلی، بدون `any`/`@ts-ignore` بی‌دلیل، بدون بازنویسی بخش نامرتبط.
